# Local overrides for 丞丞's StackChan build
# Direct connection to China VPS (bypass Cloudflare tunnel for lower latency)
CONFIG_FORCE_DEFAULT_WEBSOCKET_URL=y
CONFIG_DEFAULT_WEBSOCKET_URL="wss://xiao-memory.duckdns.org/ws"
