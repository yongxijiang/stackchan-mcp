# Local overrides for 丞丞's StackChan build
# Use Cloudflare tunnel (DuckDNS direct path has DNS issues from China ISPs)
CONFIG_FORCE_DEFAULT_WEBSOCKET_URL=y
CONFIG_DEFAULT_WEBSOCKET_URL="wss://stackchan.xiaomemory.win/ws"
